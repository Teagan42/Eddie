{
  "name": "@eddie/perf-benchmarks",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint --no-error-on-unmatched-pattern .",
    "build": "echo \"Perf benchmarks have no build step\"",
    "bench": "vitest bench"
  }
}
