{
  "name": "@eddie/perf-benchmarks",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "bench": "eslint . && vitest bench",
    "build": "tsc --project tsconfig.json",
    "test": "echo \"No tests for perf benchmarks yet\""
  }
}
