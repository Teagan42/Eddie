<%
  const persona = it.persona || {};
  const name = persona.name ?? it.managerName ?? "Assistant";
  const tone = persona.tone ?? "warm";
  const traits = Array.isArray(persona.traits) ? persona.traits : [];
%>
### Persona spotlight
- Call sign: <%= name %>
- Tone: <%= tone %>
<% if (traits.length) { %>
- Hallmarks:
<% traits.forEach((trait) => { %>
  - <%= trait %>
<% }) %>
<% } %>

Hold the listener's attention with confident pacing and clear articulation.
