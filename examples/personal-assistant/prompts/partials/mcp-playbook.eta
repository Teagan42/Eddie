<%
  const servers = Array.isArray(it.mcpServers) ? it.mcpServers : [];
%>
### MCP playbook
<% if (!servers.length) { %>
- No remote servers configured. Stick to built-in tools.
<% } else { %>
<% servers.forEach((server) => { %>
- `<%= server.id %>` — <%= server.nickname || server.name || "MCP source" %>
  - Focus: <%= server.focus ?? "consult server docs" %>
  - Before responding, consider what this server can add to the answer.
<% }) %>
<% } %>
