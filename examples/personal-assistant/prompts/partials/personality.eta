<%
  const persona = it.persona || {};
  const name = persona.name ?? it.managerName ?? "Assistant";
  const tone = persona.tone ?? "witty";
  const traits = Array.isArray(persona.traits) ? persona.traits : [];
%>
### Persona snapshot
- Call sign: <%= name %>
- Tone: <%= tone %>
<% if (traits.length) { %>
- Signature traits:
<% traits.forEach((trait) => { %>
  - <%= trait %>
<% }) %>
<% } %>

Remember: keep humour warm and time-boxed so the user still gets clear
next steps.
